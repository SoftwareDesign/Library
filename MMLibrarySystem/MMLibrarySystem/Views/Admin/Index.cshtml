@model IEnumerable<MMLibrarySystem.Models.BorrowInfo>

@{
    ViewBag.Title = "Borrow Info";
}

<table id="bookList">
    <tr>
        <td>
            Book Number
        </td>
        <td>
            Title
        </td>
        <td>
            Borrowed By
        </td>
        <td>
            Borrowed Start From
        </td>
        <td>
            State
        </td>
        <td>
            Operation
        </td>
    </tr>
    @{
        foreach (var item in Model)
        {
        <tr>
            <td>@item.Book.BookNumber
            </td>
            <td>@item.Book.BookInfo.Title
            </td>
            <td>@item.User.DisplayName
            </td>
            <td>@item.BorrowedDate
            </td>
            <td>@(item.IsCheckedOut ? "Checked Out" : "Accepted")
            </td>
            <td>
                @{
            if (item.IsCheckedOut)
            {
                    @Html.ActionLink("Return", "Return", "Admin", new { borrowId = @item.Id }, null)
            }
            else
            {
                    @Html.ActionLink("Check Out", "CheckOut", "Admin", new { borrowId = @item.Id }, null)
            }
                }
            </td>
        </tr>
      }
    }
</table>
